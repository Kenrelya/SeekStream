Pour installer gulp et bower sur votre machine

A la racine du projet pour installer les dependances:

npm install && bower install


commande `gulp` pour lancer le serveur
Site lancé sur localhost:3000
